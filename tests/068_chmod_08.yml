---

- name: setup_chmod_08
  type: setup
  tmpdirs:
    - name: 068_chmod_08_fs
      varname: config_dir
    - name: 068_chmod_08_fs_mount
      varname: mount_dir
      mode: 0755
  randnames:
    - test_volume
    - test_rg
    - test_ug
  vars:
    - name: n0
      value: $mount_dir/n0
    - name: syndicate_conf
      value: $config_dir/syndicate.conf
    - name: rg_driver
      value: $SYNDICATE_PYTHON_ROOT/syndicate/rg/drivers/disk
    - name: rg_path
      value: $SYNDICATE_RG_ROOT/syndicate-rg
    - name: ug_path
      value: $SYNDICATE_UG_ROOT/syndicatefs

- name: config
  type: sequential
  tasks:
    - name: setup
      command: $SYNDICATE_TOOL --trust_public_key -c "$syndicate_conf" --debug setup "$SYNDICATE_ADMIN" "$SYNDICATE_PRIVKEY_PATH" "$SYNDICATE_MS"

    - name: add test volume
      command: $SYNDICATE_TOOL -c "$syndicate_conf" create_volume name=$test_volume description="test volume" blocksize=4096 email=$SYNDICATE_ADMIN

    - name: create RG
      command: $SYNDICATE_TOOL -c "$syndicate_conf" create_gateway name=$test_rg type=RG volume=$test_volume email=$SYNDICATE_ADMIN private_key=auto port=31111

    - name: update RG
      command: $SYNDICATE_TOOL -c "$syndicate_conf" update_gateway name=$test_rg caps=ALL port=31112 driver=$rg_driver

    - name: create UG
      command: $SYNDICATE_TOOL -c "$syndicate_conf" create_gateway name=$test_ug type=UG volume=$test_volume email=$SYNDICATE_ADMIN private_key=auto port=31111

    - name: update UG
      command: $SYNDICATE_TOOL -c "$syndicate_conf" update_gateway name=$test_ug caps=ALL

- name: daemon UG/RG
  type: daemon
  tasks:
    - name: start RG
      command: $rg_path -c "$syndicate_conf" -d2 -f -u $SYNDICATE_ADMIN -v $test_volume -g $test_rg
      exit: 0

    - name: start UG
      command: $ug_path -c "$syndicate_conf" -d2 -f -u $SYNDICATE_ADMIN -v $test_volume -g $test_ug $mount_dir
      exit: 0

- name: check syndicate is mounted
  type: sequential
  tasks:
    - name: wait for syndicatefs to be mounted
      command: sleep 5s

    - name: test filesystem type
      command: stat -f -L -c %T $mount_dir
      compareout: "fuseblk"

- name: run chmod tests 08
  type: sequential
  tasks:
    - name: T1 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T1 - expect 0 chflags ${n0} SF_IMMUTABLE
      command: fstest chflags ${n0} SF_IMMUTABLE
      compareout: "0"

    - name: T1 - expect EPERM chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "EPERM"
      exit: 1

    - name: T1 - expect 0644 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0644"

    - name: T1 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T1 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T1 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"


    - name: T2 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T2 - expect 0 chflags ${n0} UF_IMMUTABLE
      command: fstest chflags ${n0} UF_IMMUTABLE
      compareout: "0"

    - name: T2 - expect EPERM chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "EPERM"
      exit: 1

    - name: T2 - expect 0644 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0644"

    - name: T2 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T2 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T2 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"


    - name: T3 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T3 - expect 0 chflags ${n0} SF_APPEND
      command: fstest chflags ${n0} SF_APPEND
      compareout: "0"

    - name: T3 - expect EPERM chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "EPERM"
      exit: 1

    - name: T3 - expect 0644 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0644"

    - name: T3 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T3 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T3 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"


    - name: T4 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T4 - expect 0 chflags ${n0} UF_APPEND
      command: fstest chflags ${n0} UF_APPEND
      compareout: "0"

    - name: T4 - expect EPERM chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "EPERM"
      exit: 1

    - name: T4 - expect 0644 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0644"

    - name: T4 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T4 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T4 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"


    - name: T5 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T5 - expect 0 chflags ${n0} SF_NOUNLINK
      command: fstest chflags ${n0} SF_NOUNLINK
      compareout: "0"

    - name: T5 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T5 - expect 0600 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0600"

    - name: T5 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T5 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"


    - name: T6 - expect 0 create ${n0} 0644
      command: fstest create ${n0} 0644
      compareout: "0"

    - name: T6 - expect 0 chflags ${n0} UF_NOUNLINK
      command: fstest chflags ${n0} UF_NOUNLINK
      compareout: "0"

    - name: T6 - expect 0 chmod ${n0} 0600
      command: fstest chmod ${n0} 0600
      compareout: "0"

    - name: T6 - expect 0600 stat ${n0} mode
      command: fstest stat ${n0} mode
      compareout: "0600"

    - name: T6 - expect 0 chflags ${n0} none
      command: fstest chflags ${n0} none
      compareout: "0"

    - name: T6 - expect 0 unlink ${n0}
      command: fstest unlink ${n0}
      compareout: "0"
